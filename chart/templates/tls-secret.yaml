{{- if and .Values.tls.enabled (not .Values.tls.auto) (not .Values.tls.existingSecret) }}
{{- $cn := (index .Values.ingress.hosts 0).host }}
{{- $cert := genSelfSignedCert $cn nil nil 365 }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "financial-budget.fullname" . }}-tls
  labels:
    {{- include "financial-budget.labels" . | nindent 4 }}
type: kubernetes.io/tls
data:
  tls.crt: {{ $cert.Cert | b64enc }}
  tls.key: {{ $cert.Key | b64enc }}
{{- end }}
