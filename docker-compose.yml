version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/budget.db

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
